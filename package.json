{
    "name": "@gjsify/deno_std",
    "version": "0.0.1",
    "description": "Gjsify fork of Deno standard library to make use of the node compatibility layer",
    "main": "lib/index.js",
    "module": "lib/index.js",
    "type": "module",
    "scripts": {
        "clear": "rm -rf lib",
        "print:name": "echo '@gjsify/deno_std'",
        "build": "yarn print:name && yarn clear && yarn build:gjsify && yarn build:types",
        "build:gjsify": "gjsify build --library **/*.{ts,js,mjs,mts} --log-level error",
        "build:types": "tsc --emitDeclarationOnly || exit 0",
        "build:test": "yarn build:test:gjs",
        "build:test:gjs": "gjsify build src/test.mts --app gjs --outfile test.gjs.mjs",
        "test": "yarn print:name && yarn build && yarn build:test && yarn test:gjs",
        "test:gjs": "gjs -m test.gjs.mjs",
        "git:upstream:init": "git remote add upstream https://github.com/denoland/deno_std.git || exit 0",
        "git:upstream:merge": "yarn git:upstream:init && git fetch upstream && git merge upstream/main"
    },
    "keywords": [
        "gjs",
        "node"
    ],
    "devDependencies": {
        "@gjsify/cli": "workspace:^",
        "@gjsify/unit": "workspace:^",
        "@tsconfig/deno": "^1.0.6",
        "@types/node": "^18.11.10",
        "typescript": "^4.9.3"
    }
}
